# config/

This directory holds application-level configuration constants and environment helpers.

Examples of future files:
- `app.config.ts`  — App name, base URL, feature flags
- `stripe.config.ts` — Stripe publishable key, webhook secret references
- `supabase.config.ts` — Supabase project URL / bucket names (non-secret)
